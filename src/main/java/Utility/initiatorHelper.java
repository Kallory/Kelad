package Utility;

import springGame.Kelad.Cell;

import java.util.stream.Collectors;
import java.util.stream.IntStream;

public class initiatorHelper {
//    public static List<Object> setList(Object object, Object parentObject, int objectCount) {
//        List<object> objectList = IntStream.range(0, objectCount).mapToObj(i -> object.generateRandom(parentObject)).collect(Collectors.toList());
//        return new List<object>
//    }


}
